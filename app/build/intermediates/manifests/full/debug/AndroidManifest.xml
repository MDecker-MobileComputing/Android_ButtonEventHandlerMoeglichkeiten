<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.mide.eventhandler_moeglichkeiten"
    android:installLocation="auto"
    android:versionCode="6"
    android:versionName="1.5 (September 2018)" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="28" />

    <!--
      App läuft nur auf Geräten mit mindestens API-Level 14 (Android 4.0+),
          weil ein GridLayout verwendet wird.
    -->
    <application
        android:allowBackup="true"
        android:debuggable="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:testOnly="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name="de.mide.eventhandler_moeglichkeiten.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>